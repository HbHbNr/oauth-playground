package org.hbhbnr;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class AppTest {
    @Test void constructorWorks() {
        final OAuth2Example classUnderTest = new OAuth2Example();
        assertNotNull(classUnderTest);
    }
}
